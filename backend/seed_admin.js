const mongoose = require('mongoose'); const bcrypt = require('bcrypt'); const User = require('./src/models/User'); require('dotenv').config(); mongoose.connect(process.env.MONGODB_URI, { useNewUrlParser: true, useUnifiedTopology: true }).then(async () => { const hash = await bcrypt.hash('Password@123', 10); const user = await User.findOneAndUpdate({ email: 'williamsaonen@gmail.com' }, { name: 'Williams Alfred Onen', email: 'williamsaonen@gmail.com', passwordHash: hash, role: 'admin' }, { upsert: true, new: true }); console.log('Admin user seeded successfully'); process.exit(0); }).catch(console.error);
